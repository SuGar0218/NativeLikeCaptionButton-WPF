<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:SuGarToolkit.WPF.Controls.CaptionButtons"
    xmlns:system="clr-namespace:System;assembly=netstandard">

    <system:Double x:Key="TitleBarButtonWidth">46</system:Double>
    <system:Double x:Key="TitleBarButtonIconFontSize">10</system:Double>
    <system:Double x:Key="TitleBarButtonInactiveForegroundOpacity">0.382</system:Double>
    <system:Double x:Key="TitleBarButtonPressedForegroundOpacity">0.618</system:Double>
    <system:Double x:Key="TitleBarButtonDisabledForegroundOpacity">0.382</system:Double>

    <Color x:Key="TitleBarButtonHoverBackgroundColor">#10000000</Color>
    <Color x:Key="TitleBarButtonPressedBackgroundColor">#08000000</Color>

    <SolidColorBrush x:Key="TitleBarButtonForeground" Color="{x:Static SystemColors.ActiveCaptionTextColor}" />
    <SolidColorBrush x:Key="TitleBarButtonBackground" Color="Transparent" />
    <SolidColorBrush x:Key="TitleBarButtonHoverForeground" Color="{x:Static SystemColors.ActiveCaptionTextColor}" />
    <SolidColorBrush x:Key="TitleBarButtonHoverBackground" Color="{DynamicResource TitleBarButtonHoverBackgroundColor}" />
    <SolidColorBrush
        x:Key="TitleBarButtonPressedForeground"
        Opacity="{DynamicResource TitleBarButtonPressedForegroundOpacity}"
        Color="{x:Static SystemColors.ActiveCaptionTextColor}" />
    <SolidColorBrush x:Key="TitleBarButtonPressedBackground" Color="{DynamicResource TitleBarButtonPressedBackgroundColor}" />
    <SolidColorBrush
        x:Key="TitleBarButtonDisabledForeground"
        Opacity="{DynamicResource TitleBarButtonDisabledForegroundOpacity}"
        Color="{x:Static SystemColors.ActiveCaptionTextColor}" />
    <SolidColorBrush x:Key="TitleBarButtonDisabledBackground" Color="Transparent" />

    <Style x:Key="TitleBarButtonDefaultStyle" TargetType="{x:Type local:TitleBarButton}">
        <Setter Property="Width" Value="{DynamicResource TitleBarButtonWidth}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource TitleBarButtonForeground}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:TitleBarButton}">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter
                            x:Name="PART_ContentPresenter"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsActive" Value="False">
                            <Setter TargetName="PART_ContentPresenter" Property="Opacity" Value="{DynamicResource TitleBarButtonInactiveForegroundOpacity}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{DynamicResource TitleBarButtonDisabledBackground}" />
                <Setter Property="Foreground" Value="{DynamicResource TitleBarButtonDisabledForeground}" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource TitleBarButtonHoverBackground}" />
                <Setter Property="Foreground" Value="{DynamicResource TitleBarButtonHoverForeground}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource TitleBarButtonPressedBackground}" />
                <Setter Property="Foreground" Value="{DynamicResource TitleBarButtonPressedForeground}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style BasedOn="{StaticResource TitleBarButtonDefaultStyle}" TargetType="{x:Type local:TitleBarButton}" />

</ResourceDictionary>