<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:SuGarToolkit.WPF.Controls.CaptionButtons">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/SuGarToolkit.WPF.Controls.CaptionButtons;component/CaptionButton_themeresource.xaml" />
        <!--<ResourceDictionary Source="/SuGarToolkit.WPF.Controls.CaptionButtons;component/CaptionButtonColors.Windows11.xaml" />-->
        <local:CaptionButtonColorDictionary />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="CaptionButtonDefaultStyle" TargetType="{x:Type local:CaptionButton}">
        <Setter Property="Width" Value="{StaticResource CaptionButtonWidth}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CaptionButton}">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter
                            x:Name="ContentPresenter"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Content="{TemplateBinding Content}"
                            Focusable="False" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsActive" Value="False">
                            <Setter TargetName="ContentPresenter" Property="Opacity" Value="{StaticResource CaptionButtonInactiveForegroundOpacity}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource CaptionButtonHoverBackground}" />
                <Setter Property="Foreground" Value="{StaticResource CaptionButtonHoverForeground}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{StaticResource CaptionButtonPressedBackground}" />
                <Setter Property="Foreground" Value="{StaticResource CaptionButtonPressedForeground}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{StaticResource CaptionButtonDisabledBackground}" />
                <Setter Property="Foreground" Value="{StaticResource CaptionButtonDisabledForeground}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style
        x:Key="CaptionMinimizeButtonDefaultStyle"
        BasedOn="{StaticResource CaptionButtonDefaultStyle}"
        TargetType="{x:Type local:CaptionButton}">
        <Setter Property="Content">
            <Setter.Value>
                <local:FontIcon Glyph="{StaticResource CaptionMinimizeButtonGlyph}" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style
        x:Key="CaptionMaximizeButtonDefaultStyle"
        BasedOn="{StaticResource CaptionButtonDefaultStyle}"
        TargetType="{x:Type local:CaptionButton}">
        <Setter Property="Content">
            <Setter.Value>
                <local:FontIcon Glyph="{StaticResource CaptionMaximizeButtonGlyph}" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style
        x:Key="CaptionRestoreButtonDefaultStyle"
        BasedOn="{StaticResource CaptionButtonDefaultStyle}"
        TargetType="{x:Type local:CaptionButton}">
        <Setter Property="Content">
            <Setter.Value>
                <local:FontIcon Glyph="{StaticResource CaptionRestoryButtonGlyph}" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style
        x:Key="CaptionCloseButtonDefaultStyle"
        BasedOn="{StaticResource CaptionButtonDefaultStyle}"
        TargetType="{x:Type local:CaptionButton}">
        <Setter Property="Content">
            <Setter.Value>
                <local:FontIcon Glyph="{StaticResource CaptionCloseButtonGlyph}" />
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource CaptionCloseButtonHoverBackground}" />
                <Setter Property="Foreground" Value="{StaticResource CaptionCloseButtonHoverForeground}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{StaticResource CaptionCloseButtonPressedBackground}" />
                <Setter Property="Foreground" Value="{StaticResource CaptionCloseButtonPressedForeground}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style BasedOn="{StaticResource CaptionButtonDefaultStyle}" TargetType="{x:Type local:CaptionButton}" />
    <Style BasedOn="{StaticResource CaptionMinimizeButtonDefaultStyle}" TargetType="{x:Type local:CaptionMinimizeButton}" />
    <Style BasedOn="{StaticResource CaptionMaximizeButtonDefaultStyle}" TargetType="{x:Type local:CaptionMaximizeButton}" />
    <Style BasedOn="{StaticResource CaptionRestoreButtonDefaultStyle}" TargetType="{x:Type local:CaptionRestoreButton}" />
    <Style BasedOn="{StaticResource CaptionCloseButtonDefaultStyle}" TargetType="{x:Type local:CaptionCloseButton}" />

</ResourceDictionary>